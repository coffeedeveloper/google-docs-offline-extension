# 第一版历史记录（已被模块化重构取代）

这里保留 2026-09-17 的格式化/注释方案记录，便于追溯：

- `annotations.mjs`：旧版注释插入规则，当前构建不使用。
- `static-equivalence.json`：旧版输出与原始文件的 AST/token 一致性记录，不是当前 `extension/` 的验证结果。

当前业务源码在 `src/`，当前验证结果在 `research/validation/`。不要将这里的历史哈希或等价结论用于模块化产物。
